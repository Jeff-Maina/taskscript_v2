TaskScript is a simple CLI tool for managing and tracking tasks. It uses a JSON storage system and allows for filtering, searching, parsing tasks from various sources, and exporting to different formats.

